import ShellPage from '../../components/common/ShellPage';

const StaffOrders = () => <ShellPage title="Order Management" subtitle="Verify, accept, or refuse incoming customer orders." role="STAFF" />;
export default StaffOrders;
