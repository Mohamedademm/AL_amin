import ShellPage from '../../components/common/ShellPage';

const Inventory = () => <ShellPage title="Inventory Control" subtitle="Manage stock levels across different vending spots." role="STAFF" />;
export default Inventory;
