import ShellPage from '../../components/common/ShellPage';

const StaffManagement = () => <ShellPage title="Staff Control" subtitle="Assign roles, manage permissions, and track worker performance." role="ADMIN" />;
export default StaffManagement;
