import ShellPage from '../../components/common/ShellPage';

const UserManagement = () => <ShellPage title="User Directory" subtitle="Manage client accounts, audit profiles, and handle support tickets." role="ADMIN" />;
export default UserManagement;
