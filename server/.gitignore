.vercel
dist
node_modules
.env

# Locally uploaded product images (runtime artifacts, not source)
uploads/

# Generated Prisma client — regenerated by `prisma generate` on each build.
# The hidden `.prisma/client/` directory inside node_modules is no longer used.
src/generated/prisma
